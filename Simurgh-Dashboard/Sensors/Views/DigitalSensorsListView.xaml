<!--  3. SimurghDashboard/Views/DigitalSensorsListView.xaml  -->
<UserControl
    x:Class="SimurghDashboard.Sensors.Views.DigitalSensorsListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:SimurghDashboard.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="800"
    Height="450"
    d:DataContext="{d:DesignInstance Type=viewmodels:DigitalSensorsListViewModel}"
    mc:Ignorable="d">

    <Grid Background="Transparent">
        <controls:WidgetFrame  FrameVariant="RoundedSunkenLow">

            <!--
                Using a ScrollViewer in case the number of sensors exceeds the visible space.
                VerticalScrollBarVisibility is Hidden for a clean kiosk UI, but allows touch panning.
                PanningMode ensures smooth touch scrolling on the surgical display.
            -->

            <!--
                Binding to the 'Sensors' ObservableCollection.
                Whenever the DI layer injects a new sensor via IngestMultiplexedTelemetry,
                this ItemsControl will dynamically inflate another DigitalSensorViewModel DataTemplate.
            -->
            <ItemsControl Margin="20" ItemsSource="{Binding Sensors}">

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Orientation="Vertical" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Margin" Value="10" />
                        <Setter Property="HorizontalAlignment" Value="Stretch" />
                        <Setter Property="VerticalAlignment" Value="Center" />
                    </Style>
                </ItemsControl.ItemContainerStyle>

            </ItemsControl>

        </controls:WidgetFrame>
    </Grid>
</UserControl>
